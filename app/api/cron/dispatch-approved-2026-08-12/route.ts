import { NextRequest, NextResponse } from "next/server";
import { start } from "workflow/api";
import { prisma } from "@/lib/server/db/prisma";
import { scheduledVolunteerDispatch } from "@/workflows/scheduled-volunteer-dispatch";
const CAMPAIGN_ID="338391b0-3eef-433a-bd07-b7344aae965c",CAMPAIGN_NAME="Disparo Elegiveis Sem Agendamento 1500 - 12-08-2026 08h",SCHEDULE_DATE="2026-08-12",HARD_LIMIT=1500;
function saoPauloNow(){const parts=new Intl.DateTimeFormat("en-CA",{timeZone:"America/Sao_Paulo",year:"numeric",month:"2-digit",day:"2-digit",hour:"2-digit",hour12:false}).formatToParts(new Date()),value=(type:Intl.DateTimeFormatPartTypes)=>parts.find(p=>p.type===type)?.value??"";return{date:`${value("year")}-${value("month")}-${value("day")}`,hour:value("hour")};}
export async function GET(request:NextRequest){if(!process.env.CRON_SECRET||request.headers.get("authorization")!==`Bearer ${process.env.CRON_SECRET}`)return NextResponse.json({error:"Não autorizado"},{status:401});const now=saoPauloNow();if(now.date!==SCHEDULE_DATE||now.hour!=="08")return NextResponse.json({started:false,reason:"Fora da data e hora autorizadas"});const[campaign,totalItems,uniquePhones]=await Promise.all([prisma.botCampaign.findUnique({where:{id:CAMPAIGN_ID},select:{name:true,status:true,maxSendPerRun:true,maxAttempts:true}}),prisma.botCampaignItem.count({where:{campaignId:CAMPAIGN_ID}}),prisma.$queryRaw<Array<{count:bigint}>>`select count(distinct c.phone)::bigint count from bot_campaign_items bci join case_records cr on cr.id=bci.case_id join service_orders so on so.id=cr.service_order_id join customers c on c.id=so.customer_id where bci.campaign_id=${CAMPAIGN_ID}::uuid`]);const uniquePhoneCount=Number(uniquePhones[0]?.count??0);if(!campaign||campaign.name!==CAMPAIGN_NAME||campaign.status!=="RASCUNHO"||campaign.maxSendPerRun!==HARD_LIMIT||campaign.maxAttempts!==1||totalItems!==HARD_LIMIT||uniquePhoneCount!==HARD_LIMIT)return NextResponse.json({started:false,reason:"Lote bloqueado pela validação",totalItems,uniquePhoneCount},{status:409});const claimed=await prisma.botCampaign.updateMany({where:{id:CAMPAIGN_ID,status:"RASCUNHO"},data:{status:"EM_EXECUCAO"}});if(claimed.count!==1)return NextResponse.json({started:false,reason:"Campanha já iniciada ou encerrada"},{status:409});try{const run=await start(scheduledVolunteerDispatch,[CAMPAIGN_ID]);return NextResponse.json({started:true,runId:run.runId,total:HARD_LIMIT});}catch(error){await prisma.botCampaign.updateMany({where:{id:CAMPAIGN_ID,status:"EM_EXECUCAO"},data:{status:"RASCUNHO"}});throw error;}}
