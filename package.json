{
  "name": "mhz-bot-zap",
  "version": "1.0.0",
  "private": true,
  "description": "Webhook Meta/WhatsApp com persistência em Postgres (Supabase), pronto para deploy na Vercel.",
  "main": "index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "echo \"no tests\" && exit 0"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "dotenv": "^17.4.2"
  }
}
